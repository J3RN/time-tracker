<%= form_for(@wicket) do |f| %>
  <% if @wicket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wicket.errors.count, "error") %> prohibited this wicket from being saved:</h2>

      <ul>
      <% @wicket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :part_number %><br>
    <%= f.number_field :part_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
