<!DOCTYPE html>
<html>
<head>
  <title>Time Tracker</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class='header'>
    <span class='brand'><%= link_to 'Time Tracker', '/' %></span>
    <span class='auth'>
      <% if user_signed_in? %>
        Hello, <%= link_to current_user.username, edit_user_registration_path(current_user) %>
      <% else %>
        <p>
          <%= link_to "Sign in", new_user_session_path %>
          or
          <%= link_to "Sign up", new_user_registration_path %>
        </p>
      <% end %>
    </span>
  </div>
  <div class='flashes'>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg %>
    <% end %>
  </div>

  <%= yield %>

</body>
</html>
